kind: Service
apiVersion: v1
metadata:
  name: capstone-service
  labels:
    app: capstone
    role: green
    env: prod
spec:
  type: LoadBalancer
  selector:
    app: capstone
    role: green
  ports:
    - port: 80
      targetPort: 8080
